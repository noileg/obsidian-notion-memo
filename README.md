# Notion Memo Sync（obsidian-notion-memo）

AIが一切介在しないメモを持つためのObsidianプラグイン。このプラグインを入れたvault内の
メモ（.mdファイル全部）を、Notion上の専用ページと双方向に同期する。

## 背景

個人用の自動化パイプライン「クロコ」（Notion + Gemini + Claude Codeで動く）を運用している。
スマホでのメモをNotion AIチャット経由で書くのが重い・使いにくいという不満から、
Obsidian（ノートアプリ）でスマホから気軽に書きたい、という要望が発端。

深掘りした結果、本当のニーズは「AIが一切介在しないメモを持ちたい」ことだと分かった。
そのため、このプラグインが送受信する経路には**要約・分割のようなAI処理を一切挟まない**
（Markdownの構造をそのままNotionのブロックに変換して運ぶだけ）。

設計の経緯・決めた理由は`クロコ関係\クロコ本体\記録\検討中_Obsidianメモ連携.md`に
まとまっている（クロコ側のリポジトリ）。

## 仕組み

- **対象は「このプラグインが有効なvault」全体。** フォルダによる絞り込みはしない
  （専用vaultを用意する運用が前提）。
- 保存すると数秒後（デバウンス）に自動でNotionへ送信される（設定でオフにできる）。
- Notion側の親ページ配下に、メモごとに1つの子ページとして格納される。
- Notion側の変更は、Obsidianを開いた瞬間・設定した間隔ごと（既定5分、モバイルは
  バックグラウンドでは動かない）・コマンドパレットの「Notionから同期する」・
  設定タブの「今すぐ同期」ボタン、のいずれかのタイミングで取り込まれる。
- **競合時はObsidianを正とする。** 保存後まだNotionへ送信していないメモは、
  その間にpullが走ってもローカルの内容を上書きしない（未送信の編集を優先して守る）。
  逆にpush側は、Notion側が別に更新されていても確認せず上書きする
  （本人の運用が「基本Obsidian側で編集し、Notionは表示・たまの直接編集用」の
  ため。両方を交互に編集して食い違わせた場合はObsidian側の内容が勝つ）。
- メモとNotionページの対応づけは、各ファイルのフロントマター（`notion_id`）で持つ。
  共有DBは無い。
- Markdownの見出し（`#`〜`###`、4段以上は`###`に丸める）・箇条書き（`-`）・番号付き
  リスト（`1.`）・チェックボックス（`- [ ]`/`- [x]`）・引用（`>`）・コードブロック
  （```` ``` ````）と、太字（`**`）・斜体（`*`/`_`）・インラインコード（`` ` ``）は
  Notion側の対応するブロック種別・装飾に変換される。それ以外の書式（表・ネスト・
  画像等）は今のところプレーン扱いになる。
- 異なる種類のブロックが隣り合うときは、書き戻す際に間へ空行を1つ入れる
  （Notion側の同一種別が連続する場合は詰めて出す）。空行の入り方が元の見た目と
  完全一致しないことがあるが、内容が失われることはない。

## 設定

1. Notionでintegrationを作り、「Obsidianメモ置き場（同期用）」ページに接続する
   （[Notion Integrations](https://www.notion.so/my-integrations)）。
2. このプラグインの設定タブにトークンを貼る。
3. 親ページIDは既定値（「Obsidianメモ置き場（同期用）」のID）が入っている。変える場合のみ書き換える。

## 開発

```
npm install
npm run dev    # ウォッチビルド
npm run build  # 型チェック + 本番ビルド（main.js を出力）
```

`main.js` はビルド生成物なのでリポジトリには含めない（`.gitignore`）。
