{
	"name": "obsidian-notion-memo",
	"version": "0.1.0",
	"description": "AIが介在しないメモを、専用のNotionページと双方向に同期するObsidianプラグイン",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.0.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.20.0",
		"obsidian": "^1.5.0",
		"typescript": "^5.4.0"
	}
}
